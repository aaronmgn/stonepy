# GENERATED by stonepy._generator. Do not edit by hand.
"""
List entry for ApiListFollowedUsersResponseDTO . Contains the screen name of a CI Connect user
followed by the users passed in the parameter of the service call. A list of CI Connect users
that are followed by the returned screen name is given if there exists any. Note: for use of
the CI Connect social trading platform.
"""

from __future__ import annotations

from typing import TYPE_CHECKING

from pydantic import Field

from stonepy._core.models import ResponseModel

if TYPE_CHECKING:
    from .ApiFollowerDTO import ApiFollowerDTO


class ApiUserFollowersDTO(ResponseModel):
    """
    List entry for ApiListFollowedUsersResponseDTO . Contains the screen name of a CI Connect
    user followed by the users passed in the parameter of the service call. A list of CI
    Connect users that are followed by the returned screen name is given if there exists any.
    Note: for use of the CI Connect social trading platform.
    """

    screen_name: str | None = Field(default=None, alias="ScreenName")
    """The CI Connect screen name that is followed."""
    followers: list[ApiFollowerDTO] | None = Field(default=None, alias="Followers")
    """List of CI Connect users that are followed by this screen name."""


ApiUserFollowersDTO.model_rebuild(raise_errors=False)
