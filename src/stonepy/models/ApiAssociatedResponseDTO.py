# GENERATED by stonepy._generator. Do not edit by hand.
"""
Associated order details forming part of a response DTO. An associated order is linked to a
net position rather than to specific individual trades.
"""

from __future__ import annotations

from typing import TYPE_CHECKING

from pydantic import Field

from stonepy._core.models import ResponseModel

if TYPE_CHECKING:
    from .ApiOrderResponseDTO import ApiOrderResponseDTO


class ApiAssociatedResponseDTO(ResponseModel):
    """
    Associated order details forming part of a response DTO. An associated order is linked to
    a net position rather than to specific individual trades.
    """

    stop: ApiOrderResponseDTO | None = Field(default=None, alias="Stop")
    """The associated stop order response."""
    limit: ApiOrderResponseDTO | None = Field(default=None, alias="Limit")
    """The associated limit order response."""


ApiAssociatedResponseDTO.model_rebuild(raise_errors=False)
