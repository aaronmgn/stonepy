# GENERATED by stonepy._generator. Do not edit by hand.
"""
The response from a price bar history GET request. Contains both an array of finalized price
bars, and a partial (not finalized) bar for the current period.
"""

from __future__ import annotations

from typing import TYPE_CHECKING

from pydantic import Field

from stonepy._core.models import ResponseModel

if TYPE_CHECKING:
    from .PriceBarDTO import PriceBarDTO


class GetPriceBarResponseDTO(ResponseModel):
    """
    The response from a price bar history GET request. Contains both an array of finalized
    price bars, and a partial (not finalized) bar for the current period.
    """

    price_bars: list[PriceBarDTO] | None = Field(default=None, alias="PriceBars")
    """An array of finalized price bars, sorted in ascending order based on PriceBar.BarDate ."""
    partial_price_bar: PriceBarDTO | None = Field(default=None, alias="PartialPriceBar")
    """
    The (non-finalized) price bar data for the current period (i.e, the period that has not
    yet completed) .
    """


GetPriceBarResponseDTO.model_rebuild(raise_errors=False)
