# GENERATED by stonepy._generator. Do not edit by hand.
"""The response from a simulated trade request sent by the SimulateTrade service."""

from __future__ import annotations

from decimal import Decimal
from typing import TYPE_CHECKING

from pydantic import Field

from stonepy._core.models import ResponseModel

if TYPE_CHECKING:
    from .ApiSimulateOrderResponseDTO import ApiSimulateOrderResponseDTO


class ApiSimulateTradeOrderResponseDTO(ResponseModel):
    """The response from a simulated trade request sent by the SimulateTrade service."""

    status: int | None = Field(default=None, alias="Status")
    """
    The status of the simulated order (Pending, Accepted, Open, etc.) . The table of lookup
    codes can be found at Lookup Values .
    """
    status_reason: int | None = Field(default=None, alias="StatusReason")
    """
    The ID corresponding to a more descriptive reason for the order status. The table of
    lookup codes can be found at Lookup Values .
    """
    simulated_cash: Decimal | None = Field(default=None, alias="SimulatedCash")
    """
    The simulated cash balance after executing (opening or closing) a trade. The balance may
    decrease after opening a trade (as cash may be allocated towards margin requirement) and
    increase /reduce /remain unchanged following a closure.
    """
    actual_cash: Decimal | None = Field(default=None, alias="ActualCash")
    """The actual cash balance before the trade."""
    simulated_total_margin_requirement: Decimal | None = Field(
        default=None, alias="SimulatedTotalMarginRequirement"
    )
    """The simulated total margin requirement (TMR) after executing (opening or closing) a trade."""
    actual_total_margin_requirement: Decimal | None = Field(
        default=None, alias="ActualTotalMarginRequirement"
    )
    """The actual total margin requirement (TMR) before the trade."""
    currency_id: int | None = Field(default=None, alias="CurrencyId")
    """The currency ID for the currency values in this response."""
    orders: list[ApiSimulateOrderResponseDTO] | None = Field(default=None, alias="Orders")
    """List of simulated orders with their associated response."""
    adjust: Decimal | None = Field(default=None, alias="Adjust")
    """The amount of additional spread charged to reflect trade size and underlying liquidity."""


ApiSimulateTradeOrderResponseDTO.model_rebuild(raise_errors=False)
