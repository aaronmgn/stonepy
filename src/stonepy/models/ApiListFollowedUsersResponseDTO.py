# GENERATED by stonepy._generator. Do not edit by hand.
"""
Response to a CI Connect ListFollowed service call. Contains the list of CI Connect users
followed by the users passed in the parameter of the call. Note: this API call response is for
the use of the CI Connect social trading platform and only users registered with CI Connect
are returned.
"""

from __future__ import annotations

from typing import TYPE_CHECKING

from pydantic import Field

from stonepy._core.models import ResponseModel

if TYPE_CHECKING:
    from .ApiUserFollowersDTO import ApiUserFollowersDTO


class ApiListFollowedUsersResponseDTO(ResponseModel):
    """
    Response to a CI Connect ListFollowed service call. Contains the list of CI Connect users
    followed by the users passed in the parameter of the call. Note: this API call response is
    for the use of the CI Connect social trading platform and only users registered with CI
    Connect are returned.
    """

    following_users: ApiUserFollowersDTO | None = Field(default=None, alias="FollowingUsers")
    """The list of users followed by the supplied screen names."""


ApiListFollowedUsersResponseDTO.model_rebuild(raise_errors=False)
