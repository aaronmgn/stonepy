# GENERATED by stonepy._generator. Do not edit by hand.
"""Resource method: GetPriceBarsAfterDate."""

from __future__ import annotations

from stonepy._core.resource import BaseResource
from stonepy._endpoints import market as _ep
from stonepy.models import GetPriceBarResponseDTO


class _GetPriceBarsAfterDateMixin(BaseResource):
    def get_price_bars_after_date(
        self,
        market_id: str,
        interval: str,
        span: int,
        from_timestamp_utc: int,
        price_type: str,
        *,
        max_results: int | None = None,
    ) -> GetPriceBarResponseDTO:
        """
        Get historic price bars for the specified market in OHLC (open, high, low, close)
        format, suitable for plotting in candlestick charts. Returns price bars in
        ascending order after the user given fromTimestampUTC . When there are no prices
        for a particular time period, no price bar is returned. Thus, it can appear that
        the array of price bars has "gaps", i.e. the gap between the date & time of each
        price bar might not be equal to interval x span. Sample Urls:
        /market/1234/barhistoryafter?interval=MINUTE&span=15&fromTimestampUTC=1390402198 /
        market/735/barhistoryafter?interval=HOUR&span=1&fromTimestampUTC=1390402198&maxRes
        ults=10
        /market/1577/barhistoryafter?interval=DAY&span=1&fromTimestampUTC=1390402198
        """
        return _ep.get_price_bars_after_date(
            self._ctx,
            market_id,
            interval,
            span,
            from_timestamp_utc,
            price_type,
            max_results=max_results,
        )
